import { styles } from "./Detail"
// import { useDetailContext } from "../../context/detailsContext";
import { Box } from "@mui/material";

const Detail = () => {
//   const { detail, setDetail } = useDetailContext();
  return (
    <Box></Box>
  )
}

export default Detail