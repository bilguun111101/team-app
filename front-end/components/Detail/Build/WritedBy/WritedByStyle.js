export const style = {
  writedBySection: (theme) => ({
    gap: "1em",
    width: "90%",
    display: "flex",
    alignItems: "center",
  }),
  avatarAndName: (theme) => ({
    gap: "2px",
    display: "flex",
    flexDirection: "column",
  }),
  name: (theme) => ({
    color: "#6d7d8b",
    fontSize: "14px",
  }),
};
