export const style = {
  name: (theme) => ({
    color: "#6d7d8b",
    fontSize: "14px",
  }),
  chatInput: (theme) => ({
    gap: "1em",
    width: "100%",
    display: "flex",
    marginTop: "1em",
  }),
  sendButton: (theme) => ({
    display: "flex",
    justifyContent: "flex-start",
  }),
};
